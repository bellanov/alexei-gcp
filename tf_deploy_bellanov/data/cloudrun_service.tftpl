
{
  spec {
    containers {

      image = "${image}"

      %{ for env_key, env_value in env }
      env {
        name = "${env_key}"
        value = "${env_value}"
      }
      %{ endfor ~}

    }

    service_account_name = "${service_account}"
  }
}